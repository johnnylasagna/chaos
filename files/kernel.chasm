LABEL calls
    CMPI A 0
    JE loop

    CMPI A 1
    JE print

    CMPI A 2
    JE read

    CMPI A 3
    JE write

LABEL print
    SYS_PRINT
    KRET

LABEL read
    SYS_READ
    KRET

LABEL write
    SYS_WRITE
    KRET

LABEL loop
    JMP loop

HALT
